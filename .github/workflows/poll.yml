name: Poll API Every 10 Minutes

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: # 👈 This allows you to trigger it manually

jobs:
  fetch_data:
    runs-on: ubuntu-latest
    steps:
      - name: Send API Request
        run: |
          curl -X GET "https://rohanchauhan.me"
